const checker = function(input) {

	return (
	  input.length === 10 &&
	  (input4] === 'c') == ('1'==1) &&
		input2].charCodeAt(0) % (true+true) &&
		input.substring(0, 2) == '\x6d\x40' &&
		input.substring(8, 10).split('').reverse().join('') == 's3' &&
		input3] ^ 50 === 51
	);
};
